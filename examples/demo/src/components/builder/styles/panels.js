export const panelStyles = ({
    // For categorized 
    // panels, such as 
    // sizing or spacing
    // panels.
    category: ({
        display: 'inline',

    }),
    // For a particular field 
    // with own label.
    label: ({
        marginRight: '1rem',
        flex: '0 1 25%'
    }),
    input: ({
        // As important to override
        // material UI rules.
        flex: '1 1 0'
    }),
    input_color: ({
        maxWidth: '150px',
    }),
    field: ({
        display: 'flex',
        alignItems: 'center',
        margin: '1rem 0rem'
    }),
});

